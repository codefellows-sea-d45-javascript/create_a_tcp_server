var expect = require('chai').expect;
var fs = require('fs'); 
var tcp = require(__dirname + '/../lib/tcp_server'); 


describe('test for server starting', function(){
	it('should do something', function(){
		
	});
});

describe('a net server request', function() {
  it('should ...', function() {
   
  });
});
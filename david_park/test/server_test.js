var expect = require('chai').expect;
var data = require(__dirname + '/../data')

describe('', function() {
  it('should test a new written file', function() {

    expect()

    //expect server to receive request through socket
    //expect new file to be added to /../data
  })
})

var expect = require('chai').expect;
var net = require('net');
var fs = require('fs');
var server = require(__dirname + '/../server');

var connect = 'http://localhost:3000/'

describe('the server', function() {
  before(function() {

  }
  it('should write requests to new file', function(){
    expect()to.eql();
  });
});
